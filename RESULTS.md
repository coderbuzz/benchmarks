# Benchmark Results

Results are collected per environment. Each entry includes the machine specs,
runtime versions, and test date.

---

_No results yet. Run `bash packages/<pkg>/<bench>/run.sh` to generate._
